@model IEnumerable<Sinem.Models.Usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de usuarios</h2>

<p>
    <img src="/Content/Plus_16px.png" />
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Direccion
        </th>

        <th>
            @Html.DisplayNameFor(model => model.tipoIdentificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.identificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.apellido)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.fechaNacimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @((ViewBag.Direcciones as List<Sinem.Models.Direccion>).Where(x => x.idDireccion == item.idDireccion).FirstOrDefault().descripcion)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.tipoIdentificacion)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.identificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.apellido)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.fechaNacimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario)
        </td>
        <td>
            @if (ViewBag.Usuario.idUsuario != item.idUsuario)
            {
                <img src="/Content/Edit_16px.png" />
                @Html.ActionLink("Editar", "Edit", new { id = item.idUsuario })

            }
            |
            <img src="/Content/Edit_16px.png" />
            @Html.ActionLink("Editar datos de contacto", "EditContact", new { id = item.idUsuario })
            |
            <img src="/Content/Identification Documents_16px.png" />
            @Html.ActionLink("Ver detalles", "Details", new { id = item.idUsuario }) |
            @if (ViewBag.Usuario.idUsuario != item.idUsuario)
            {

                @Html.ActionLink(item.estado == "activo" ? "desactivar" : "activar", "Delete", new { id = item.idUsuario })
            }
        </td>

    </tr>
}

</table>
